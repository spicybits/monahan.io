<%= Rouge::Themes::ThankfulEyes.render(:scope => '.highlight') %>

pre.highlight {
	padding: 5px 10px;
	font-size: 15px;
  overflow: auto;
  word-wrap: normal;
  overflow-wrap: normal;  /* word-wrap is being renamed in CSS3 */
  white-space: pre;
}